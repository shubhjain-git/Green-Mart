# API Gateway Configuration
server:
  port: 8080

spring:
  application:
    name: api-gateway
    
  cloud:
    gateway:
      # Enable service discovery
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      
      # Default filters for all routes
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Credentials Access-Control-Allow-Origin
      
      # Route definitions
      routes:
        # Auth Service Routes (PUBLIC - no authentication required)
        - id: auth-service
          uri: lb://AUTH-SERVICE
          predicates:
            - Path=/api/auth/**
          filters:
            - RewritePath=/api/auth/(?<segment>.*), /api/auth/${segment}
        
        # Product Service Routes (PUBLIC - GET requests, authenticated for others)
        - id: product-service-public
          uri: lb://PRODUCT-SERVICE
          predicates:
            - Path=/api/products/**
            - Method=GET
          filters:
            - RewritePath=/api/products/(?<segment>.*), /api/products/${segment}
        
        - id: product-service-protected
          uri: lb://PRODUCT-SERVICE
          predicates:
            - Path=/api/products/**
            - Method=POST,PUT,DELETE,PATCH
          filters:
            - JwtAuthenticationFilter
            - RewritePath=/api/products/(?<segment>.*), /api/products/${segment}
        
        # User Service Routes (PROTECTED)
        - id: user-service
          uri: lb://USER-SERVICE
          predicates:
            - Path=/api/users/**
          filters:
            - JwtAuthenticationFilter
            - RewritePath=/api/users/(?<segment>.*), /api/users/${segment}
        
        # Inventory Service Routes - Health endpoint public
        - id: inventory-service-health
          uri: lb://INVENTORY-SERVICE
          predicates:
            - Path=/api/inventory/health
          filters:
            - RewritePath=/api/inventory/(?<segment>.*), /api/inventory/${segment}
        
        # Inventory Service Routes (PROTECTED)
        - id: inventory-service
          uri: lb://INVENTORY-SERVICE
          predicates:
            - Path=/api/inventory/**
          filters:
            - JwtAuthenticationFilter
            - RewritePath=/api/inventory/(?<segment>.*), /api/inventory/${segment}
        
        # Order Service Routes - Health endpoint public
        - id: order-service-health
          uri: lb://ORDER-SERVICE
          predicates:
            - Path=/api/orders/health
          filters:
            - RewritePath=/api/orders/(?<segment>.*), /api/orders/${segment}
        
        # Order Service Routes (PROTECTED)
        - id: order-service
          uri: lb://ORDER-SERVICE
          predicates:
            - Path=/api/orders/**
          filters:
            - JwtAuthenticationFilter
            - RewritePath=/api/orders/(?<segment>.*), /api/orders/${segment}
        
        # Checkout Service Routes - Health endpoint public
        - id: checkout-service-health
          uri: lb://CHECKOUT-SERVICE
          predicates:
            - Path=/api/checkout/health
          filters:
            - RewritePath=/api/checkout/(?<segment>.*), /api/checkout/${segment}
        
        # Checkout Service Routes (PROTECTED)
        - id: checkout-service
          uri: lb://CHECKOUT-SERVICE
          predicates:
            - Path=/api/checkout/**
          filters:
            - JwtAuthenticationFilter
            - RewritePath=/api/checkout/(?<segment>.*), /api/checkout/${segment}
        
        # Payment Service Routes - Health endpoint public
        - id: payment-service-health
          uri: lb://PAYMENT-SERVICE
          predicates:
            - Path=/api/payments/health
          filters:
            - RewritePath=/api/payments/(?<segment>.*), /api/payments/${segment}
        
        # Payment Service Routes (PROTECTED)
        - id: payment-service
          uri: lb://PAYMENT-SERVICE
          predicates:
            - Path=/api/payments/**
          filters:
            - JwtAuthenticationFilter
            - RewritePath=/api/payments/(?<segment>.*), /api/payments/${segment}
      
      # Global CORS Configuration
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins:
              - "http://localhost:3000"
              - "http://localhost:5173"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - PATCH
              - OPTIONS
            allowedHeaders:
              - "*"
            allowCredentials: true
            maxAge: 3600

# JWT Configuration (same secret as auth-service)
jwt:
  secret: ${JWT_SECRET:YWJjZGVmZ2hpamtsbW5vcHFyc3R1dnd4eXoxMjM0NTY3ODkwYWJjZGVmZ2hpamtsbW5vcHFyc3R1dnd4eXo=}

# Eureka Client Configuration
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER:http://localhost:8761/eureka/}
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true

# Actuator endpoints
management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway,metrics
  endpoint:
    gateway:
      enabled: true
    health:
      show-details: always

# Resilience4j Circuit Breaker Configuration
resilience4j:
  circuitbreaker:
    configs:
      default:
        slidingWindowSize: 10
        failureRateThreshold: 50
        waitDurationInOpenState: 10000
        permittedNumberOfCallsInHalfOpenState: 5
  timelimiter:
    configs:
      default:
        timeoutDuration: 3s

# Logging
logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    com.greenmart.gateway: DEBUG
    reactor.netty: INFO
