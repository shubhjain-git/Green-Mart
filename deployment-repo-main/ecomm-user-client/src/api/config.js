/**
 * API configuration for deployment-repo backend.
 * All services are accessed via API Gateway at /api
 *
 * Gateway: http://localhost:8080 (dev) | http://68.183.86.246:8080 (prod)
 * Services: auth, users, products, inventory, orders, checkout, payments
 */

export const API_BASE = '/api';
